# 一分钟上手

## 1. 装依赖

```powershell
pip install -r requirements.txt
```

## 2. 填密钥

复制模板，填入你自己的 key（vivo / ECNU / 智谱 GLM 三选一即可）：

```powershell
Copy-Item .env.example .env
notepad .env
```

最少只需填一组。例如用 vivo：

```
LLM_PROVIDER=vivo
VIVO_AIGC_API_KEY=你的key
VIVO_AIGC_APP_ID=你的appid
```

## 3. 跑一章试试

在 `submission_llm_core/` 目录下执行：

```powershell
python -m backend.llm_dispatcher chapter --source-path 神游.txt --source-file-id demo --book-title 神游 --provider vivo
```

跑完会在 `local_cache/<书名>_<id>/extraction_json/` 下生成逐章结构化 JSON，终端打印结果摘要。

## 4. 三种任务

```powershell
# 章节提取（原文 → 逐章 JSON）
python -m backend.llm_dispatcher chapter --source-path 神游.txt --source-file-id demo --book-title 神游

# 整书汇总（逐章 JSON → 角色/物品/剧情/世界观汇总）
python -m backend.llm_dispatcher summary --chapter-json-dir local_cache/神游_demo/extraction_json --source-file-id demo --book-title 神游

# 角色卡（逐章 JSON → 指定角色的 Markdown 卡）
python -m backend.llm_dispatcher card --chapter-json-dir local_cache/神游_demo/extraction_json --source-file-id demo --character-name 石野
```

## 常见问题

- **报 `requires api_key`**：`.env` 没填对应 provider 的 key，或 `--provider` 与填的 key 不匹配。
- **中文原文乱码**：确保装了 `chardet`（requirements 已含），它会自动识别 GB18030 等编码并转 UTF-8。
- **`ModuleNotFoundError: backend`**：命令没在 `submission_llm_core/` 目录下执行。
